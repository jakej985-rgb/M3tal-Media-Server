name: m3tal-core
arch: amd64
platform: linux
version: ${VERSION}

section: utils
priority: optional
maintainer: Jake Johnson
description: Lightweight M3tal Core CLI
license: MIT

contents:
  - src: ./dist/m3tal
    dst: /usr/bin/m3tal

scripts:
  postinstall: ./scripts/postinstall.sh
